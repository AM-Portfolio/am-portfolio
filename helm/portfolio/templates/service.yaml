apiVersion: v1
kind: Service
metadata:
  name: {{ include "am-market-data.fullname" . }}
  labels:
    {{- include "am-market-data.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "am-market-data.selectorLabels" . | nindent 4 }}
