# Last valueFile in Argo Application — overrides am-gitops image-tags while values revision is this branch.
# Tag MUST stay quoted — unquoted CI run ids become YAML floats (3.55e+10 → InvalidImageName).
global:
  image:
    tag: "35598269041"
