name: Portfolio Common Lib Publish

on:
  push:
    branches: ["main", "develop", "test/**"]
    paths:
      - "am-common-data/**"
  pull_request:
    branches: ["main", "develop", "test/**"]
    paths:
      - "am-common-data/**"
  workflow_dispatch:

jobs:
  publish:
    name: Publish Common Data Lib
    permissions:
      contents: write
      packages: write
    uses: AM-Portfolio/am-pipelines/.github/workflows/publish-library.yml@develop
    with:
      language: "java"
      working_directory: "am-common-data"
      sdk_name: "am-common-data-lib"
      maven_repo_id: "github"
      maven_repo_url: "https://maven.pkg.github.com/AM-Portfolio/am-portfolio"
    secrets: inherit
